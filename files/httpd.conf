# INTENTIONALLY INVALID — for testing `httpd -t` / config validation failures.
# Do not deploy to production as-is.

ServerRoot "/etc/httpd"
Listen 80
Include conf.modules.d/*.conf

User apache
Group apache

# Error: not a valid Apache directive (httpd will reject this on syntax check).
InvalidApacheDirectiveThatWillFail on

<Directory />
    AllowOverride none
    Require all denied
</Directory>

DocumentRoot "/var/www/html"

<Directory "/var/www/html">
    AllowOverride None
    Require all granted
</Directory>
